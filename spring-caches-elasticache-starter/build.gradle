description = "Starter for using Spring Caches with AWS ElastiCache"

dependencies {
    api(project(":spring-caches-starter"))
    api(project(":spring-caches-elasticache-autoconfigure"))
    api(project(":backends:elasticache-cache-provider"))
}